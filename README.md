# One key to Docker-compose

### How to use

* Run the following command in terminal.
```
git clone https://github.com/bear3z/onekey_to_docker-compose && sudo sh onekey_to_docker-compose/install.sh
```